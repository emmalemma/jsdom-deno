{
  "name": "jsdom-deno_dependency-list",
  "private": "Not exactly, but to treat this as a node package would be an affront to sensibility. By npm spec this file must be named package.json, but it's just a list of vendored dependencies found under node_modules. Any other relevant metadata is outside the scope of this file.",
  "dependencies": {
    "jsdom": "^16.3.0",
    "url": "^0.11.0"
  }
}
